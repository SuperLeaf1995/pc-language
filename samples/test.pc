/*XOR_OR_MOV.PC*/

/*Optimizer puts MOV (r), 0 as XOR (r), (r) as a optimization*/
/*Must be converted to this (excluding comments):

[section .text]

_entryPoint:
	mov al, 0 ;NOT OPTIMIZED
	ret ;THE RETURN INSTRUCTION

*/

routine entryPoint(byte a, byte b, byte c) {
	copy(0,ra8); /*Set AL as 0*/
	return; /*return to caller*/
}
